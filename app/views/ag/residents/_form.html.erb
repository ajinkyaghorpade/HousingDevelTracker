<%= form_with(model: ag_resident, local: true) do |form| %>
  <% if ag_resident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ag_resident.errors.count, "error") %> prohibited this ag_resident from being saved:</h2>

      <ul>
      <% ag_resident.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ag_resident_name %>
  </div>

  <div class="field">
    <%= form.label :dob %>
    <%= form.date_select :dob, id: :ag_resident_dob, start_year: (Date.today.year - 100) %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender, id: :ag_resident_gender %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
