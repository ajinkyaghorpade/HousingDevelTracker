<%= form_with(model: ag_country, local: true) do |form| %>
  <% if ag_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ag_country.errors.count, "error") %> prohibited this ag_country from being saved:</h2>

      <ul>
      <% ag_country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ag_country_name %>
  </div>

  <div class="field">
    <%= form.label :iso %>
    <%= form.text_field :iso, id: :ag_country_iso %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
