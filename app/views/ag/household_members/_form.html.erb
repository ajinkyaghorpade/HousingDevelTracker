<%= form_with(model: ag_household_member, local: true) do |form| %>
  <% if ag_household_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ag_household_member.errors.count, "error") %> prohibited this ag_household_member from being saved:</h2>

      <ul>
      <% ag_household_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ag_household_id %>
    <%= form.number_field :ag_household_id, id: :ag_household_member_household_id %>
  </div>

  <div class="field">
    <%= form.label :ag_resident_id %>
    <%= form.number_field :ag_resident_id, id: :ag_household_member_member_id %>
  </div>

  <div class="field">
    <%= form.label :head %>
    <%= form.check_box :head, id: :ag_household_member_head %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
